apiVersion: apps/v1
kind: Deployment
metadata:
  name: progress-service
spec:
  selector:
    matchLabels:
      app: progress-service
  template:
    metadata:
      labels:
        app: progress-service
    spec:
      containers:
        - name: progress-service
          image: localhost:54886/progress-service
          env:
            - name: NATS_URL
              value: "nats://nats:4222"
---
apiVersion: v1
kind: Service
metadata:
  name: progress-service
spec:
  ports:
    - port: 8080
      targetPort: 8080
  selector:
    app: progress-service